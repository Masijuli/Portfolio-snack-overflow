# Snack Overflow

## Run It 
0. in bash run `python -m pip install flask "psycopg[binary]"` to install dependencies
1. run `python app.py` in bash terminal
2. navigate to http://127.0.0.1:5000

## Tech Stack
Front End
- HTML
- CSS
- JS

Database
- Postgress
- hosted on Neon

Connection between database and front end
- Python flask app